import { View, Text } from "react-native";
import React from "react";

const TextChatShowCard = ({ item }: any) => {
  console.log(item);
  return (
    <View className="w-full h-10 bg-white">
      <Text>TextChatShowCard</Text>
    </View>
  );
};

export default TextChatShowCard;
